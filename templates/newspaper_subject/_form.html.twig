{{ form_start(formSubject) }}

    <div class="form-group row">
        {{ form_label(formSubject.title, 'Indiquez le sujet :') }}
        <div class="col-sm-10">{{ form_widget(formSubject.title) }}</div>

        <small>{{ form_help(formSubject.title) }}</small>

        <div class="form-error">
            {{ form_errors(formSubject.title) }}
        </div>
    </div>

    <div class="form-group row">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            {% include 'tools/_backButton.html.twig' with {
                'text': 'Annuler',
                'path': path('newspaper_edit', {'id':newspaper.id})
            } %}

            {% include 'tools/_formAdminValidateButton.html.twig' with {
            } %}
        </div>
    </div>

{{ form_end(formSubject) }}


