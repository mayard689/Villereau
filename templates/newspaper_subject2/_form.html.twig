{{ form_start(formSubject) }}

    <div class="form-group row">
        {{ form_label(formSubject.content, 'Choisissez un article :') }}
        <div class="col-sm-10">{{ form_widget(formSubject.content) }}</div>

        <small>{{ form_help(formSubject.content) }}</small>

        <div class="form-error">
            {{ form_errors(formSubject.content) }}
        </div>
    </div>

    <div class="form-group row">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            {% include 'tools/_backButton.html.twig' with {
                'text': 'Annuler',
                'path': path('newspaper2_edit', {'id':newspaper2.id})
            } %}

            {% include 'tools/_formAdminValidateButton.html.twig' with {
            } %}
        </div>
    </div>


{{ form_end(formSubject) }}
