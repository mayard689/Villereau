<section>
    <div class="container">

        {%  include 'tools/_h1.html.twig' with {'title' : 'INFORMATIONS'} %}

        <div class="row">
            {% for content in contents %}
                {{ include('content/_contentCard2.html.twig') }}
            {% endfor %}
        </div>

    </div>
</section>
