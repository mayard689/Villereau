{% extends 'base.html.twig' %}

{% block title %}Event{% endblock %}

{% block body %}
    <section class="container">
        <h1>Event</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ event.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ event.name }}</td>
                </tr>
                <tr>
                    <th>Texte</th>
                    <td>{{ event.text }}</td>
                </tr>
                <tr>
                    <th>Date</th>
                    <td>{{ event.date ? event.date|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Lieu</th>
                    <td>{{ event.place }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('event_index') }}">Voir la liste des évenements</a>

        <a href="{{ path('event_edit', {'id': event.id}) }}">Modifier</a>

        {{ include('event/_delete_form.html.twig') }}
    </section>
{% endblock %}
